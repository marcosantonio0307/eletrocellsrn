<div class="label">
	<span>Meus Clientes</span>
</div>

<%= form_tag search_clients_path, method: :get, class: 'navbar-form navbar-right' do %>
	<div class="search">
		<%= text_field_tag :name, @name, class: 'form-control' %>
		<%= button_to 'Buscar', nil, class: 'btn btn-success' %>
	</div>
<% end %> 

<%= render 'clients' %>